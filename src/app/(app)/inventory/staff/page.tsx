import { InventoryManager } from '@/components/inventory/InventoryManager';

export const metadata = { title: 'Staff — Edison Helpdesk' };

export default function StaffInventoryPage() {
  return <InventoryManager section="staff" />;
}
