import { InventoryManager } from '@/components/inventory/InventoryManager';

export const metadata = { title: 'Students — Edison Helpdesk' };

export default function StudentsInventoryPage() {
  return <InventoryManager section="students" />;
}
